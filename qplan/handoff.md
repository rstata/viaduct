# Qplan Current Handoff

## Working State

Context-parameter removal is complete across every semantics source set. The final 29 correctness declarations now receive operation/world dependencies explicitly or retain them in private `ResolverConformanceLogic`, `ResolverDemandValidationLogic`, and `ResolverReplayLogic` implementations. Existing result/resolver receivers remain; witness creation, the operation-aware cache factory, and root-reference replay are `SharedOperationContext<*>` extensions. Each result judgment retains one cache shared by demand and conformance checks; nested Query results create separate caches while sharing the same reference witness. Standalone predicates and repeated judgments preserve fresh state, and neither cache nor witness moves into operation state. Callers in fixtures, tests, and JMH no longer establish semantics contexts. The ten remaining local context blocks call unchanged model APIs, so the compiler flag stays. All qplan main, test, test-fixture, and JMH source sets compile, and 80 focused tests passed (72 semantics correctness/replay/witness/caller tests and eight arbitrary witness tests; `/tmp/2rv-qplan-correctness-context-validation.log`). The existing selective-replay test now also checks cache independence across repeated judgments and standalone predicates. No broad generated or end-to-end suites ran. Local docs record the completed migration. Earlier materialization work is committed; this correctness conversion remains uncommitted. The completed temporary plan remains untracked and must not be committed.

The Slate inventory update could not be confirmed: the edit call and subsequent verification read both remained pending without returning and were stopped. Before retrying, read the current Logic and Follow-ups sections. The intended update adds `ResolverConformanceLogic`(p), `ResolverDemandValidationLogic`(p), and `ResolverReplayLogic`(p), documents their per-result cache and shared reference-witness lifetimes, and marks context-parameter removal complete with the 80-test validation evidence. The last confirmed document version before this attempt was 38; avoid duplicating the update if the server applied the pending write.

Shared `materializeResult` now takes `cycleChecker` as its final optional argument, defaulting to `CycleCheckState.createNOP()`. Nested `ctx.query` result projection, correctness replay, and witness reconstruction omit that argument. Both families' runtime `materializeResolverInput` APIs continue to require an explicit checker, preserving real checking for coroutine resolver inputs (including inputs within a nested query) and explicit no-op checking for DFS. The nested-query projection itself previously passed the operation's checker and now uses the no-op default; the private materialization algorithms are unchanged. KDoc, local documentation, and Slate explain the distinction. Every qplan source set compiled and all 62 focused materialization/caller tests passed (`/tmp/2rv-qplan-materialization-default-checker-validation.log`), including explicit cycle-check coverage. All work remains uncommitted and the temporary plan remains untracked.

Materialization now exposes three explicit API roles: `semantics.shared.materializeResult` projects existing selected OER values for nested `ctx.query` results and correctness replay across all families; `semantics.resolvers.materializeResolverInput` is the thin runtime-input wrapper for Resolver01–23; and Resolver26 retains its distinct runtime `materializeResolverInput`, which can reserve symbolic cells and value promises before producers install them. Shared projection requires installed cells and promises, can await unfinished values and bindings, prefers exact symbolic keys before falling back to grounded stored keys, and never rekeys cells. Replay reconstructs deterministic resolver inputs from existing results, including Resolver26 results retaining symbolic identities after bindings resolve. KDoc, local documentation, and the Slate inventory explain these roles and distinguish nested `ctx.query` calls from declared Query fragments. Both private logic implementations and all checker choices are unchanged. Every qplan main, test, test-fixture, and JMH source set compiled, and 62 focused materialization/caller tests passed (`/tmp/2rv-qplan-materialization-naming-all-sources.log`, `/tmp/2rv-qplan-materialization-naming-focused-tests.log`); no broad generated or end-to-end suites ran. Materialization work remains uncommitted, and `context-parameter-removal-plan.md` remains untracked and must not be committed.

The two-commit migration history is rewritten: `dc94e23b3` includes the straightforward context-parameter conversion and operation-extension entry points; `7d6d86ff4` replays the demand-closure/sibling-dependency extraction on top. Both rewritten commits compiled every qplan source set and passed focused tests (83 for the first, 46 for the second; `/tmp/2rv-qplan-restack-first-validation.log` and `/tmp/2rv-qplan-restack-second-validation.log`). The second commit's Kotlin patch is byte-for-byte identical to the original. The later materialization work was restored uncommitted; at that checkpoint, all final source contents matched the pre-rewrite snapshot.

Resolver execution entry points now use the operation as their extension receiver: 18 declarations converted in the first context-parameter commit (`resolve`, `resolveObserved`, and `startResolve` across all maintained families), plus Resolver26's already-explicit `OperationContext.startResolve` helper, for 19 declarations. The extensions stay in their resolver-family packages. Callers now write `operation.resolve(selections)` or `operation.startResolve(...)`; 91 calls were migrated and 14 forwarding `let` scopes were removed. Existing data-oriented receivers and ordinary operation/world arguments remain on selection, key, value, and materialization helpers. Request scopes, concrete operation ownership, demand-policy captures, and dispatcher/state identities are unchanged. All qplan main, test, test-fixture, and JMH sources compiled, and 83 focused tests passed across all ten resolver families, coroutine lifecycle, Resolver26 startup/nested execution, inclusion conditions, and request ownership (`/tmp/2rv-qplan-operation-extensions-final-compile.log`, `/tmp/2rv-qplan-operation-extensions-focused-tests.log`). The operation-extension adjustment is included in the first migration commit; materialization remains uncommitted, and the plan remains untracked. The remaining 29 context-parameter declarations are still confined to correctness judgments.

Materialization no longer declares context parameters. The internal top-level extensions `materializeResult` and `materializeResolverInput` take explicit `operation` and `cycleChecker` arguments and delegate to file-private `MaterializationLogic` in `semantics.shared` and `ResolverInputMaterializationLogic` in `semantics.resolver26`. Each logic instance retains the two stable dependencies through recursive helpers. Shared materialization still reads installed cells and stored keys; Resolver26 input materialization still reserves symbolic cells and value promises. Live coroutine callers retain their operation's checker; DFS, correctness replay, and witness reconstruction retain their no-op checker choices. Redundant materialization context scopes are removed, model contexts and correctness cache/witness lifetimes are preserved, and the Slate inventory includes both new private classes. All qplan main, test, test-fixture, and JMH sources compiled, and 66 focused materialization, inclusion-condition, coroutine, DFS, and correctness tests passed (`/tmp/2rv-qplan-materialization-compile.log`, `/tmp/2rv-qplan-materialization-focused-tests.log`). Twenty-nine context-parameter declarations remain, all in correctness judgments. The inventory/plan document remains untracked.

The straightforward context-parameter conversion is committed as `dc94e23b3`: 57 main declarations and all 25 supporting fixture/test/JMH declarations now use explicit operation/world dependencies, with operation receivers on resolver entry points. The second change replaces the six demand-closure/sibling-dependency declarations with `semantics.resolvers.ResolverDemandClosureLogic` and `semantics.resolvers.resolver01.SiblingDependencyLogic`. These retain existing operations and OER occurrences, keeping changing expansion/order accumulators local; the unused type-only closure entry point is retired. The new logic classes name their OER constructor properties `oerOccurrence`; matching named arguments and examples use that spelling. All qplan source sets compiled after the naming change (`/tmp/2rv-qplan-oer-occurrence-rename-compile.log`); tests were not rerun for this naming-only amendment. That checkpoint left forty contextual main declarations in materialization and correctness, with checker/cache/witness lifetimes unchanged. All qplan source sets compile, all 35 focused sibling/DFS/coroutine tests passed, and full baseline, first-conversion, between-commit, and extraction checks passed. Validation logs are `/tmp/2rv-qplan-context-baseline-check.log`, `/tmp/2rv-qplan-context-stage1-check.log`, `/tmp/2rv-qplan-context-between-commits-check.log`, `/tmp/2rv-qplan-context-stage2-compile.log`, `/tmp/2rv-qplan-context-stage2-focused-tests.log`, and `/tmp/2rv-qplan-context-stage2-check.log`. The working inventory remains untracked and must not be committed.

Passive traversal types now use logic-role names consistently: `SharedPassiveValueResolutionLogic`, Resolver26's private `PassiveValueResolutionLogic`, `CoroutinePassiveValueResolutionLogic`, and `DepthFirstPassiveValueResolutionLogic`. Their source files, corresponding test classes/files, imports, callers, local documentation, and Slate inventory use these names. `FieldResolutionLogic` retains its name because it handles resolver invocations, reference hops, and conditioned passive values before delegating value traversal; the two responsibilities are layered. Functions retain their existing verb-based names such as `resolvePassiveValues`. All qplan main, test, test-fixture, and JMH sources compiled (`/tmp/2rv-qplan-passive-logic-names-validation.log`); tests were skipped for this naming-only change. Historical profiling captures retain their original symbols.

Demand-closure metadata now uses `ClosedInputDemandContext`, with KDoc stating its organizing principle: inputs established by demand closure for one object orchestration, retained through binding declaration, dispatch validation, and field installation. One planned field-path read that produces an instantiated variable binding is a `VariableProviderReadOccurrence`. Properties use `variableProviderRead` as their naming stem (`variableProviderReads` and `variableProviderReadsByResolverOccurrence`); locals and parameters use `providerRead` or `providerReads`. The property KDoc preserves the distinction between object-fragment reads stored on the publication/closure and Query-fragment reads prepared separately. Local documentation and the Slate inventory are updated. All qplan main, test, test-fixture, and JMH sources compiled (`/tmp/2rv-qplan-variable-provider-names-validation.log`); tests were skipped for this naming-only change.

The class-to-interface audit is complete: `CoroutineOperationContext`, `DepthFirstOperationContext`, and `GroundedFieldPublicationOccurrence` are concrete classes again, and all callers use their constructors. Their shared operation properties still come from delegation, preserving dispatcher and state identities; passive traversal is initialized with the concrete owning instance. `FieldPublicationOccurrence` was already restored to a class. Of the six class-to-interface conversions made for context delegation, only `SharedOperationContext` and Resolver26's `OperationContext` remain interfaces because they still serve as delegation targets. The remaining local delegate in Resolver26's factory is named `operationDelegate`, as requested. All qplan main, test, test-fixture, and JMH sources compiled, and 41 focused coroutine, depth-first, and passive-resolution tests passed (`/tmp/2rv-qplan-concrete-contexts-validation.log`).

`FieldPublicationOccurrence` is a concrete class again, constructed directly and still implementing `OperationContext by operation`. Its interface and anonymous factory existed only so a field task could delegate to the publication type; tasks now retain publications through composition. KDoc, local documentation, and the Slate inventory reflect the class. All qplan main, test, test-fixture, and JMH sources compiled, and ten focused Resolver26 orchestration and nested-execution tests passed (`/tmp/2rv-qplan-publication-class-validation.log`). The audit also identified `GroundedFieldPublicationOccurrence`, `DepthFirstOperationContext`, and `CoroutineOperationContext`; the subsequent cleanup recorded above restored them to classes. Keep `SharedOperationContext` and Resolver26's `OperationContext` as interfaces because they still serve as targets of the retained publication-to-operation delegation.

Tasks now supply their contexts through composition: `SharedOrchestrationTask<O>` exposes `operation: O`, and `SharedFieldResolverTask<P>` exposes `publication: P`. Both shared contracts and all concrete task implementations have had operation/publication subtyping removed to keep algorithmic work distinct from the contexts it uses. Type parameters needed only for task subtyping were removed; concrete owner typing and shared coroutine lifecycle bases remain. The publication-to-operation subtype relationship stays, as do the `SharedOperationContext` interface, its static anonymous `create(...)` factories, and specialized operation contexts with shared-property delegation. The grounded publication keeps family dispatch fully typed through `operation.dispatcher`; its inherited dispatcher uses the common shared bound. Resolver26 child scopes replace scheduling and retain state identities. All qplan main, test, test-fixture, and JMH sources compiled, and 62 focused tests passed, including publication delegation, nested-scope ownership/cancellation, all four coroutine families, DFS preparation, and shared passive traversal (`/tmp/2rv-qplan-task-context-composition-validation.log`). KDoc, local documentation, and the Slate inventory reflect this decision.

Retain `SharedFieldResolverTask` deliberately as an architectural contract. Resolver26 is the intended end product; maintaining Resolver01–23 preserves its architectural integrity by forcing careful decomposition and encapsulation across simpler algorithms and different execution structures. Shared interfaces and members may enforce these relationships without current polymorphic consumers. The field-task/publication relationship is important enough to preserve for that purpose. Its KDoc and the project, semantics, resolver-version, design-principle, and context documentation now record this rationale. The sharing audit is complete; the Slate inventory retains this decision and the completed “Per-resolver additions to shared interfaces” comparison section has been removed.

`ValueSourceOccurrence` is the agreed name for the sealed resolver-invocation, reference, and conditioned-passive-value hierarchy, with `sourceOccurrence` retained as the property shorthand. A `FieldPublicationOccurrence` retains its destination cell and initial `ValueSourceOccurrence`. In the case of references, resolution may transitively reach additional value sources before completing that cell's value slot. The README and Slate keep this relationship description general: direct reference hops invoke field resolvers, while transitive processing of their outputs can reach other value-source kinds. All qplan main, test, test-fixture, and JMH sources compiled after this naming-only change (`/tmp/2rv-qplan-value-source-validation.log`); no tests were rerun.

The agreed type-naming convention treats context as the broad role: an immutable bundle of configuration and state references with a defining organizing principle. Occurrence contexts use `Occurrence`; logic contexts use `Task` or `Logic`; contexts organized around another principle use `Context` and explain that principle in KDoc. Compose focused contexts to supply an algorithm's larger needs. An occurrence context may include operation/configuration/state references; their presence alone does not justify the generic `Context` suffix. The convention is applied to the six occurrence-context types: `OEROccurrence`, `FieldResolverOccurrence`, `ValueSourceOccurrence`, `FieldPublicationOccurrence`, `GroundedFieldPublicationOccurrence`, and `SharedFieldPublicationOccurrence`. The sealed value-source interface covers invocation, reference, and passive-value sources and is exposed as `sourceOccurrence`. Operation contexts retain `Context`, with explicit organizing-principle KDoc. Publication occurrence and resolver-invocation occurrence remain distinct: one publication can involve several reference-hop invocations. Local documentation and the Slate inventory use the new names. All qplan main, test, test-fixture, and JMH sources compiled, and 26 focused orchestration, coroutine-lifecycle, request-ownership, and DFS tests passed (`/tmp/2rv-qplan-occurrence-names-validation.log`).

The semantics naming audit is applied across all main sources and dependent callers: eleven suffix-bearing variable/property names now use their semantic roles, including `operation`, `publication`, distinct OER/resolver/field-resolver occurrences, `fieldResolverOccurrences`, and `variableBindings`. Resolver26's binding-declaration readiness property is `bindingsState`, as requested. All thirteen bare `context` declarations (the original twelve plus a missed cancellation callback parameter) have role names; the grounded coroutine collection is `publications`. That variable-name pass preserved type names and standard `coroutineContext` terminology; the subsequent type renames are recorded above. DFS field execution uses `with(publication)` rather than publication/operation/occurrence aliases. All qplan main, test, test-fixture, and JMH sources compiled, and thirteen focused DFS construction, scheduling, reference-publication, and argument-error tests passed (`/tmp/2rv-qplan-role-names-validation.log`). The Slate inventory is updated in place, and `context-params.md` records the naming convention.

Resolver01–23 now share `semantics.resolvers.GroundedFieldPublicationOccurrence<O>`, a concrete immutable implementation of the shared publication contract holding the same eight grounded-publication inputs. DFS and coroutine field tasks retain it through their typed `publication`; DFS retains the separate publication occurrence through composition. DFS dispatchers synchronously call `DepthFirstFieldResolverTask.create(publication)` before execution or enqueue, preserving pre-freeze promise claims and activation. The reactor retains task-completion accounting; repeated field publication is rejected by the cell’s one-shot writer claim. Coroutine preparation and Resolver26’s specialized publication context remain intact. All qplan main, test, test-fixture, and JMH sources compiled, and 73 focused lifecycle, ordering, freezing, and reference-publication tests passed (`/tmp/2rv-qplan-grounded-publication-validation.log`). The shared context, task composition, and preparation boundary are documented in `semantics/README.md`.

Coroutine implementation consolidation now shares request-root scheduling, the field-task run/error boundary, and the orchestration dispatch/install/freeze lifecycle between Resolver21–23 and Resolver26. All three implementations live in `semantics.resolver26`; no shared interface was added. Concrete orchestration tasks extend `CoroutineOrchestrationTask<O>` and retain their own demand preparation, active-work predicate, and field installation; Resolver26 also retains passive-field validation and its private binding-declaration guard. The base owns the typed operation/occurrence/source, atomic one-shot dispatch guard, and install-before-freeze sequence. `CoroutineTaskDispatcher` calls it directly, eliminating three callbacks. Duplicate dispatch uses Resolver26’s `IllegalArgumentException` convention in both coroutine families. All qplan main, test, test-fixture, and JMH sources compiled and 51 focused orchestration, coroutine-lifecycle, passive-traversal, and ownership tests passed (`/tmp/2rv-qplan-shared-orchestration-validation.log`). The preceding dispatcher/field-task extraction passed 62 focused tests; broad tests remain deferred. The Slate inventory reflects both changes.

Consolidation preference: always favor aligning Resolver21–23 with Resolver26 over Resolver01–08 when choosing a shared implementation. The grounded-orchestration alternative was not pursued.

Package policy: reserve `semantics.shared` for all-family code, use `semantics.resolvers` for code shared by Resolver01–23, and use `semantics.resolver26` for code shared by Resolver21–23 and Resolver26. Private implementation details may remain in `semantics.shared` regardless of their consumers; do not relocate or flag them solely for narrower use. The existing public/internal violations flagged for later cleanup are `inputParentDemand` and `installParentBackedgeFields`, which serve Resolver22/23 and 26. Keep the private `CycleCheckStateImpl` and private parent-demand support in shared; the cycle-checking interface is all-family support. Common materialization/correctness callers justify keeping grounding helpers shared even when particular resolver algorithms do not call them directly. `ObjectSelectionForest.fetchBindings` has no callers in qplan, and `Arguments.fetchBindings` is used only by that unused helper; review those separately.

Qplan is a compiling Kotlin model of Viaduct query field resolution. Resolver26 is the primary algorithm and eventual implementation blueprint; the maintained earlier resolvers form the semantic and execution comparison grid documented in [`resolver-versions.md`](./resolver-versions.md).

All maintained resolvers use `shared/SharedPassiveValueResolutionLogic`, prepared `SharedOrchestrationTask` factories, and `SharedOperationContext.dispatcher`. Resolver21-23 now use `CoroutineOperationContext`, `CoroutineOrchestrationTask`, `CoroutineFieldResolverTask`, and `FieldResolutionLogic` under `resolvers/resolver21`, corresponding to the Resolver26 classes, plus `GroundedFieldPublicationOccurrence` shared with the depth-first families under `resolvers`. Both coroutine families now use `resolver26/CoroutineTaskDispatcher`, and their concrete orchestration and field tasks extend `resolver26/CoroutineOrchestrationTask` and `resolver26/CoroutineFieldResolverTask`, respectively. Their factory closes grounded demand, binds `FromArgument` variables, and installs parent backedges before passive descent; orchestration installs field promises and freezes the OER. Both coroutine implementations dispatch orchestration and field tasks on the request root and retain Query-fragment producers under the owning field task. Query producers return `Deferred<EngineObjectOrErrorData>` directly to the owning invocation; Resolver26 starts them during field-task setup so runtime binding dependencies can progress before activation or materialization waits. Resolver21-23 and Resolver26 publish exceptions from field tasks and Query-fragment producers as field errors, while actual coroutine cancellation and JVM Errors propagate. Unexpected non-cancellation exceptions escaping launched orchestration tasks fail the request in all four resolvers. Resolver26 additionally retains runtime binding/activation protocols. The old passive traversal, ancestor re-orchestration, child rediscovery, duplicated parent-demand implementation, and duplicated passive-value tests have been removed. Shared tests cover the common traversal. Resolver01-03 and Resolver06-08 continue to share the concrete depth-first tasks with recursive and queued dispatch; `DepthFirstResolve.resolve` creates an empty Query source internally and dispatches root orchestration directly, so every orchestration task has a non-null source. Access checks remain future work: Resolver21-23 are the intended initial implementation before transfer to Resolver26.

`SharedOperationContext<D>` preserves the dispatcher type, so shared passive resolution dispatches through its operation without downcasts. Resolver26 and Resolver21-23 instantiate `resolver26.CoroutineTaskDispatcher<O, F>` with their concrete orchestration and publication-context types; both depth-first implementations accept `SharedTaskDispatcher<DepthFirstOrchestrationTask, GroundedFieldPublicationOccurrence<DepthFirstOperationContext>>`. All three dispatcher implementations accept their concrete orchestration and field types. Semantic helpers that do not dispatch use `SharedOperationContext<*>`, and the static `SharedOperationContext.create(world)` factory creates a standalone operation context without a dispatcher.

The shared task contracts now preserve concrete context types: `SharedOrchestrationTask<O>` exposes its owning `operation`, `SharedFieldPublicationOccurrence<O, D>` types its existing operation property, and `SharedFieldResolverTask<P>` retains the concrete `publication`; callers reach the operation and OER occurrence through it. Resolver26 retains its renamed `FieldPublicationOccurrence` intact in the running task; Resolver01–23 retain the shared `GroundedFieldPublicationOccurrence<O>` with their concrete operation type. `OrchestrationTask` privately owns `bindingDeclarationStarted`, leaving `ClosedInputDemandContext` structurally immutable. The [Resolver26 class and interface inventory](https://slate.airbnb.tools/1moxjEe27Q) records the current usage-based categories and deferred instrumentation/worker-dispatcher review. This refactoring compiled all qplan main, test, test-fixture, and JMH sources and passed the focused passive-resolution, depth-first dispatch, coroutine lifecycle, and Resolver26 orchestration/start tests; broad suites were intentionally deferred.

Resolver01–23 already obtain `Assumptions` through their operation contexts. Their obsolete world-context wrappers have been removed from demand-policy callbacks, dependency ordering, orchestration, and Query-fragment production; field tasks establish the model context only around resolver invocation. The depth-first field task no longer has a private `world` alias. Pure parent-input-demand analysis still receives `operation.world` at its call boundary. All qplan source sets compiled after this cleanup, and 38 focused demand, scheduling, and coroutine lifecycle tests passed.

The context-alias audit removed 30 unnecessary shortcut or duplicate property declarations across context, task, resolution-logic, and passive-traversal implementations, including the former ad-hoc `OrchestrationTask.world` alias. Tasks follow their context properties: operation configuration through `operation.world`, and field-task data through `publication`. Publication occurrences retain their deliberate delegation of the operation interface. `FieldResolutionLogic` receives only its task, without a duplicate publication-cell argument. `SharedPassiveValueResolutionLogic<T, O>` preserves concrete operation typing in its one shared property, eliminating three subclass duplicates. Unused selection/path/type/demand members were removed from `SharedFieldPublicationOccurrence`; Resolver26 already consumes them from its occurrence context, and earlier resolver contexts retain their directly owned selection/path/type values. Useful projection declarations remain under the common contracts their consumers actually use: closed demand, occurrence publication metadata, and scheduling paths. The subsequent grounded-publication consolidation removed the former depth-first self-publication adapter. The completed audit section was removed from the Slate inventory at the user’s request; [`context-params.md`](./context-params.md#context-property-ownership) records the ownership rule. Full qplan source compilation and 75 focused traversal, task-lifecycle, and root-reference tests passed after the audit; broad suites remain deferred.

Mutable interpretation state no longer lives in `model.Assumptions`. Shared semantics uses a `SharedOperationContext` containing the immutable `world: Assumptions`, `VariableBindingsState`, and semantically passive resolver observer. Resolver26 extends that boundary with its request scope and explicit `cycleChecker: CycleCheckState` and `bindingsState: BindingDeclarationsState` properties; the operation context does not implement those state protocols. `OEROccurrence` carries the stable root, exact path, target, and optional structural parent bundle through Resolver26 object orchestration and field-resolution tasks.

`FieldResolver` now owns a suspending, selection-sensitive function relation. Suspension is an execution capability rather than part of the modeled mathematical signature. Every field and node resolver function also receives an explicit `ResolutionExecutionContext`, whose abstract `resolveSelectionSet(MaterializeSelectionForest)` operation is the nested Query-selection boundary. Test-fixture overloads adapt ordinary property and semantic resolver functions by ignoring this argument. Resolver26 passes the owning concrete `FieldResolverTask`, while Resolver01-08, Resolver21-23, and correctness reapplication pass `ResolutionExecutionContext.Unsupported`. Resolver21-23 and Resolver26 invoke the relation directly from their field-task coroutines; Resolver01-08 and synchronous correctness reapplication use localized `runBlocking` boundaries. `FieldResolver.of` adapts existing non-selective functions by projecting their stable output in selective worlds, while `FieldResolver.ofSelective` passes runtime `SelectionForest` demand directly to a selective function without post-projection. Test-fixture registry assembly supports both forms. Resolver26 deliberately applies each producer once with the union of its bounded client and object-RSS demand; production tests that instead expose consumer-shaped applications remain disabled with passing one-shot `ALTERNATIVE` counterparts. The lasting rationale for using `of` in resolver-algorithm tests, reserving `ofSelective` for integration paths, reapplying the resolver relation during correctness validation, and treating supplied demand as a separate witness property is recorded in [`semantics/testing-contracts.md`](./semantics/testing-contracts.md#resolver-fixture-and-oracle-boundary).

Argument-bearing fields remain outside the resolver-output relation, but `FieldResolver.evaluateRelation` no longer recursively revalidates every returned object and list at runtime. Fixture construction remains responsible for producing argumentless passive output fields; the former runtime-rejection test is retained as an ignored specification test. The performance rationale and controlled measurements are recorded in [`semantics/resolver-profiling.md`](./semantics/resolver-profiling.md#2026-09-04-002258-utc).

The execution feature-test adapter now assembles selective field and node executors as selection-sensitive qplan resolvers and converts their Resolver26 successor demand into Engine API selection sets. The conversion preserves concrete applicability, nested fields, resolved arguments, lowered `V_A_typename`, and the source coordinates of Node-valued fields. It also honors `EngineConfiguration.fieldSelectivityProvider` when executor metadata does not declare selectivity. Engine-managed selective-node demand containing only the authoritative `id` returns an empty payload shell without invoking tenant node code; typename remains generated. Selective node output is projected at ingress to demanded top-level fields with registered field-resolver fields removed and omitted demanded nullable fields filled with null, retaining production ownership and coverage without admitting surplus or missing output into qplan. Twenty-one formerly `Selective`-only field tests run unchanged (18 selective-field tests and 3 required-selection tests), and 28 production selective-node tests now run unchanged. Production tests whose expected response is supported but whose callback-owned RSS, consumer-shaped execution, or repeated-materialization policy is not retain their original form as disabled `ALT` tests with passing `ALTERNATIVE` counterparts. All eighteen former `SelSem` cases in `SelectiveFieldResolversExecutionTest.kt` now have one-shot alternatives that preserve final GraphQL data and meaningful error paths while removing production materialization-cache state, invocation-order, and snapshot-reconciliation assertions. The alternatives use declarative field providers, no-RSS function providers, bounded one-shot over-fetching, stable producer results, and active resolution of argument-bearing fields. Remaining callback-RSS N/A cases require child-produced variables beneath `@parent` to parameterize ancestor work or require pruning an unused provider that qplan rejects at registry construction.

Seven production-derived conditional-directive execution tests now run unchanged. Source-document conversion preserves source response keys such as `__typename` after lowering, while static registry dependency and branch-order validation exclude `Never` selections. Of the three formerly disabled selective-field cases, two now pass unchanged; the third is an intentional sometimes-passive ownership difference and has a same-response one-shot `ALTERNATIVE` whose selective parent omits the registered descendant. The production-specific `fetchAs` null-versus-unset behavior for a conditionally excluded RSS field remains an intentional adapter difference rather than part of Resolver26 activation semantics.

Every reasoning world uses one canonical lowered `viaduct.graphql.schema.ViaductSchema`. Fields, arguments, enum values, object types, type conditions, possible-object-type sets, type expressions, resolver keys, selections, and EOD schema types come from that schema instance. Model, fixtures, arbitrary generation, semantics, and execution use `ViaductSchema` and its flat `TypeExpr` representation directly.

The model distinguishes `EngineInputData`, `EngineOutputData`, `ResolverOutputData`, and `EngineResult`. `ResolverOutputData ::= EngineOutputData | RootFieldReferenceData`, keeping symbolic references in the resolver codomain without admitting them to resolver input data. Engine results are finite graphs whose ordinary containment edges are well-founded; `ObjectEngineResult.ParentKey` is the sole distinguished backedge and references the immediate structural ancestor OER. Structural traversals skip parent keys, while finite selection-driven materialization follows them into fresh EOD projections. OER cells and paths otherwise use canonical `ObjectEngineResult.ObjectKey` values, which may retain occurrence-specific variables in their arguments. `GroundKey` remains the refinement for operations that require resolved argument values. A `ResolverOccurrenceId` combines its Query-rooted OER identity with its exact structural path; the primary result roots primary occurrences and every independently executed query fragment roots its subordinate occurrences in its own fresh Query OER. Variable instances retain their defining `ResolverOccurrenceId`; keys carry no separate occurrence identity. Result union is deliberately rejected for parent-bearing graphs until it can copy and retarget backedges correctly.

Each `EngineResultCell` now carries separate field- and type-checker-result promises typed as production `CheckerResult?`. A completed null means that no checker applies, while an unpublished promise remains distinct so later checker-resolution work can claim it. Factory-built completed cells default both checker results to null; pending list cells remain activated for value publication but leave both checker slots unclaimed. Checker promises have the same activation-aware, one-shot completion and cancellation boundary as value promises. Resolver input materialization still reads only value slots, and no maintained resolver currently claims or publishes checker slots.

The registry contains field-checker and reusable checker-RSS scaffolding but no checker execution semantics. `CheckerRequiredSelections` retains separate object-rooted and Query-rooted maps whose names are globally disjoint. Object entries may be null to request an empty containing-object value; Query entries are non-null. It retains every named materialization selection independently and exposes the construction-selection union for each root. `FieldChecker` adds field arguments and the checker function; a future type-checker model should reuse `CheckerRequiredSelections`.

Checker execution should begin in Resolver21-23 and Resolver26 rather than Resolver01-08. The depth-first attempt demonstrated that resolving all values before all checkers works only while resolver materialization ignores access results; once ordinary resolver inputs enforce checks, value and checker slots must be scheduled together. The initial structured-suspension milestone should still only resolve checker slots and leave both checker and resolver input materialization value-only. Subsequent enforcement must distinguish checker RSS as raw-only demand from ordinary resolver RSS as checked demand, without making unchecked behavior transitive across a value-resolver boundary.

Broader checker validation remains future work: `correctResolution` deliberately ignores checker slots, generated worlds do not contain checkers, and there is no exact checker-application observer or witness. Before checker support is considered complete, add a semantically passive checker observer that records attempted field/type checker applications by exact Query root, occurrence path, arguments, and checker kind; add a sibling exact-application judgment rather than folding operational minimality into value correctness; and add a focused generated checker profile with an independent expected checker-demand set. Its coverage guards should require object- and Query-rooted checker RSS, active and passive checked fields, checker-only intermediate fields, resolver dependencies reached from checker demand, overlapping raw and checked demand for the same occurrence, fresh checker Query OERs, repeated/list occurrences, and eventually one type-check application per object-valued base cell. The overlap case must establish that an independently client- or resolver-demanded field executes its checker exactly once even when a checker also reads that field raw.

`@skip` and `@include` are lowered into `InclusionCondition`: one source occurrence is conjunctive, conflicting requirements are `Never`, and equal merged keys use a local disjunction while each occurrence's condition is pushed into only its own descendants. Resolver26 closes and launches infrastructure tasks conservatively, evaluates each merged condition after its bindings arrive, and invokes tenant resolver code only for a positively activated cell. Demand closure tracks the satisfiable disjuncts already propagated for each expanded resolver key, so an equal key that gains a late alternative also broadens its object-fragment and provider prerequisites to the same fixed point. A negatively activated cell remains physically reserved but is logically absent; resolver-input materialization filters conditioned source occurrences before response-key collection. Object- and Query-fragment provider paths remain available to decide conditions that consume `FromObjectField` or `FromQueryField` variables. Passive resolution still follows resolver source presence rather than interpreting conditions independently, and condition-aware selective-output projection remains deferred.

## Source And Lowered Schema Boundary

Fixture composition retains two distinct schemas:

- The source `GraphQLSchema` owns external GraphQL parsing, validation, source spread rules, source output types, coercion where required, and response completion.
- The canonical lowered `ViaductSchema` owns qplan fields, types, selections, resolver registry entries, conformance checks, values, and subtype reasoning. Source-backed definitions retain exact source GraphQL-Java EOD witnesses; synthetic typename definitions remain model-internal.

`GJSchema.fromSDL` parses and validates the source schema and lowers it to the final canonical `ViaductSchema`. `SourceSchemaAdapter` is the explicit source-to-lowered boundary and requires the canonical source/lowered fixture pair. Node-valued source fields retain their schema coordinates. Source-shaped node references are normalized to root-field references targeting the built-in `Query.node`; that resolver decodes the concrete type and original ID and dispatches to the matching node resolver. Typename remains compiled into ordinary synthetic field resolvers before semantic reasoning.

## Current Semantic Capabilities

All maintained resolvers support source-sensitive ownership of argumentless registered fields: an ancestor output may supply such a field passively, while an absent field uses its standard resolver. Fields with arguments are always active. The exact contract and resolver support matrix live in [`semantics/testing-contracts.md`](./semantics/testing-contracts.md).

Resolver22/23 and Resolver26 support `@parent` fields. Schema preparation validates an argument-free singular parent field and maps it to its unique argument-free child producer; child producers may return singular objects, lists, or nested lists. Resolver input validation rejects variables on every concrete branch reachable beneath a parent selection, including parent fields retargeted from abstract coordinates, while allowing variables guarded by concrete branches disjoint from all parent retargets. It otherwise preserves selective-resolver support. Each child parent cell points to its actual containing ancestor OER, but EOD materialization creates a fresh finite parent projection rather than preserving resolver-visible object identity. One-level demand lifting composes for grandparent selections. Conservative lifting may over-resolve or over-materialize ancestor demand when an active grandchild field is dynamically passive; this is an accepted tradeoff for the uncommon overlap of those features. The focused Resolver26 parent property test classifies exact variable-bearing argument selections reached across resolver boundaries beneath parents by fragment, depth, and binding-source combination, and measures diagonal dependencies in which a resolver selected below one parent independently requires its own parent.

Resolver01-03 and Resolver06-08 intentionally reject `@parent` demand. Parent-induced dependencies can require an ancestor resolver to re-enter the same still-open descendant occurrence, which cannot be represented by their local sibling ordering without adding occurrence-aware suspension or graph re-entry machinery. The maintained depth-first versions stay small for comparison and future proof work; [`examples.md`](./examples.md#why-the-depth-first-resolvers-do-not-support-parent) contains the concrete counterexample.

Resolver26 closes each OER once. Successor demand statically transposes descendant parent demand into selective producer output, while input-demand closure independently discovers parent-induced construction demand in requested descendants and reachable resolver inputs and transposes it across matching producer edges. Passive resolution therefore passes construction demand unchanged into each returned OER rather than mixing in successor invocation demand. This makes all ancestor work discoverable before child construction while retaining source-sensitive suppression of standard resolver demand. Every OER freezes after its one local launch cycle.

Resolver02/03, Resolver07/08, Resolver22/23, and Resolver26 support independently resolved Query-rooted resolver fragments. Each application receives a fresh Query OER materialized with response keys preserved, and correctness validation retains that OER as an occurrence-specific witness. The general distinction between variable producers and fragment consumers is defined in [`semantics/README.md`](./semantics/README.md#variable-production-and-consumption): `FromObjectField` and `FromQueryField` choose the provider fragment, while either the object fragment or Query fragment may consume either binding. Resolver26 implements both provider kinds and all three consumption shapes.

All maintained resolvers now support symbolic root-field references whose arguments are grounded and variable-free. A reference is executed only when its source position is demanded; a reference-bearing field omitted from non-selective demand is skipped entirely, including when the reference is a list element. Source presence takes precedence over registry membership, so a reference supplied for an argumentless registered field suppresses that field's standard resolver and input demand. Every hop uses a fresh empty Query OER for identity, the descriptor's canonical path and grounded arguments, and an empty object input typed as the target's containing type. Targets must have empty object fragments. Resolver01-23 admit only `FromArgument`; Resolver02/03, Resolver07/08, and Resolver22/23 also execute target Query fragments, while Resolver03/08/23 supply full successor demand. Direct reference results are followed as tails. Resolver01-08 perform each hop inline before continuing depth-first sibling work; Resolver21-23 perform it under their existing structured coroutine scope. Referenced composite outputs retain detached descendant OERs for ordinary active resolution even though their symbolic source cannot be structurally revisited.

Unlike production's object-only `LazyEngineObjectData` representation, a qplan reference target may return a singular scalar or enum as well as a composite object because the resolved value is published directly into the consumer cell. Ordinary abstract targets are admitted conservatively before invocation only when all of their possible concrete object types belong to the consumer's possible object types; partial overlap is rejected. A canonical `Query.node` reference instead uses the validated concrete type encoded in its ID for that same conformance check, while the resolved runtime value is still checked against the consumer field type. Every resolver family retains the originating node ID as authoritative when node dispatch returns another root-field-reference tail. Resolver26 reference targets may additionally retain `FromQueryField` and provider variables, and namespace-relative requirements must be written as Query RSS with the Query-to-namespace path prefixed.

Resolver26 implements that common behavior through its conditioned passive occurrences, `FieldResolverTask`, and `FieldResolutionLogic`. After ordinary active-field demand reaches its fixed point, each object orchestration scans its directly demanded source fields once for reference carriers; closure itself can add active fields but cannot create new references. `ValueSourceOccurrence` carries the publication selection, path, expected type, and construction demand. Each OER with active work launches its own request-root orchestration task, while a passive OER with no work freezes synchronously. The orchestration root is retained as an explicit placeholder and ownership boundary for possible future asynchronous orchestration; the current implementation does not require it to suspend, and its undispatched body synchronously installs each publication through `FieldResolverTask.launchAll`, launches its request-root field tasks, freezes the OER, and completes. Each field task owns setup and launches its Query-fragment and object-provider producers as children of its field scope. `FieldResolutionLogic` holds the concrete task as a constructor property and handles ordinary object fields, object-field references, list-element references, and direct reference results using one reference-tail loop. Resolver26 does not expose the raw request scope through its operation context: `CoroutineTaskDispatcher` is the narrow request-root capability, accepts only orchestration and field-resolution contexts, and intentionally provides no generic `launch`; `RequestScopeOwnershipTest` locks the permitted source locations. Each tail hop creates another fresh identity root while retaining the original publication identity. These roots are intentionally never shared or deduplicated; production's physical deduplication and support for target object RSS are outside qplan's semantic model. Resolver observations retain the invocation/publication link so correctness replay and application accounting can follow every hop without treating a finalized OER as relaunchable.

Every maintained resolver supports canonical `FromArgument` paths through nested input objects, with null propagation through nullable intermediate objects and no list traversal. Resolver26 additionally evaluates compiled `FromObjectField` provider paths at runtime and evaluates compiled `FromQueryField` provider paths in each occurrence's fresh Query OER. Resolver26 preserves symbolic OER keys and treats grounding as readiness and invocation data rather than a rekeying operation; distinct symbolic keys may therefore invoke the same grounded field arguments more than once.

Resolver26 supports a third `FromProvider` variable source backed by one optional suspending function on `FieldResolver`. Each active resolver occurrence invokes that function exactly once after grounding its own arguments, validates that the returned names exactly match its provider declarations, and completes the occurrence-local bindings before materializing either resolver fragment. Object- and Query-fragment work remains independently scheduled and suspends on those bindings. Ordinary provider failures, including task-local `CancellationException`s thrown while the field task remains active, complete the owning resolver field with causal error metadata and unblock dependent variable reads with error bindings. Actual request cancellation cancels outstanding field and binding promises with `CancellationException`; field-task-root completion handlers own cleanup, including cancellation before entry and bindings created by reference hops, while Kotlin propagates cancellation to helper coroutines and Query-producer `Deferred` values. `Error`s are not caught or converted by Resolver26. The durable protocol and canonical field-task boundary are recorded in [`semantics/resolver26/design.md`](./semantics/src/main/kotlin/semantics/resolver26/design.md#failure-and-cancellation-protocol).

Arbitrary registry generation has an independent `ResolverFromProviderVariablesEnabled` control. It assigns schema-compatible scalar and nested-list values derived deterministically from the owning resolver's grounded argument fingerprint, composes every generated name for one resolver into its single callback, and preserves the plans in benchmark-corpus serialization. Resolver26's `from-provider` property profile isolates this source in ordinary object fragments and hard-requires generated argument-sensitive providers, runtime applications of their owning resolvers, and observed argument-dependent callback values; deterministic contracts retain the object/Query-fragment interaction coverage. Every serialized Resolver26 broad-campaign profile also enables provider-variable generation, and the balanced profile requires runtime application of a provider-owning resolver as structural coverage evidence.

Qplan's generated property suite has a `node` profile across the maintained resolver grid and a Resolver26-only `selective-node` profile. `SelectiveNodeResolversEnabled` constructs selection-aware node dispatchers from the same complete deterministic `ObjectPlan` relation used by ordinary node properties; the callback receives node-owned demand and model-owned nonselective projection preserves the independent oracle boundary. The profile captures supplied demand and hard-requires an activated node resolver with nonempty demand, while the focused model fixture test verifies exact callback demand delivery.

The schema-embedded resolver test DSL declares deterministic provider variables with `providerVars: {name: value}`. Provider values accept GraphQL literals and the existing argument expression strings. `VariablesProviderResolverContract`, enabled only for Resolver26, covers argument-derived and multiple provider values across distinct occurrences, null bindings, provider failures, and the completed occurrence-local binding state.

Ordinary generated tests include a replayable `sometimes-passive` profile across the full maintained resolver grid. Resolver23 and Resolver26 deep stress profiles generate a schema-valid parent spine whose deepest resolver reads through three parent edges to its great-grandparent; query generation activates that path, variable insertion excludes every argument occurrence beneath a parent selection, and resolver value plans retain source-supplied `@parent` fields. The dedicated `resolver26ParentFocused` task requires generated evidence of those source-supplied fields and runs a `40:5:5` product as four reported 250-case slices. It supplements the fixed witness with two randomized parent chains of depth three or four per schema, singular and nested-list producers, nullable forms, union parent targets, scalar siblings, recursively composed diagonal demands, and directed object/Query variable attempts on those witnesses. When sometimes-passive generation is enabled, every random-parent type also contributes an argumentless constant resolver with unused parent input so ancestor-supplied values exercise speculative parent fetching without changing resolver semantics. The harness joins exact registered and applied occurrence IDs to report skipped standard resolvers with parent demand and hard-requires this intersection. Its application observer retains each fragment's materialization selections and reports per-slice `HIT`/`MISS` results plus, for each of nine runtime coverage criteria, the number of completely hitting slices and the number of contributing generated cases combined and by slice; the criteria span topology, resolver placement, variable sources and pairs, input locations, argument depth, diagonals, source/location combinations on variable-bearing diagonals, and sometimes-passive parent demand. Individual-slice misses remain diagnostic, but a miss after combining all four slices fails the task. A Resolver26-only deterministic contract independently covers Query-fragment variable use on diagonal parent demand for `FromArgument`, `FromObjectField`, and `FromQueryField`. Resolver26 broad campaign profiles also enable sometimes-passive generation and require independent evidence that registered result occurrences were supplied by their source owner without invoking the standard field resolver. Resolver26 application accounting reconstructs registered occurrences across the primary result and every request-local Query-fragment OER, qualifies observed applications by Query root and exact path, and counts sometimes-passive occurrences as the difference from that complete rooted ledger. Resolver26 application observations expose exact applied occurrence sets to from-field binding validation, which requires all and only the `FromObjectField` and `FromQueryField` bindings of applied occurrences and traverses every request-local Query root.

Ordinary generated tests now run a base-tier `root-field-reference` profile across the full maintained resolver grid. It disables fragments and variables, forces empty target fragments, and requires both generated and observed references while exercising the fixed consumer's scalar, enum, object, union, list, override, and tail positions. Resolver26's stronger exact ordinary-application-count assertion is omitted only for this profile because the consumer deliberately supplies registered fields passively; its whole-result, permutation, activation, and from-field-binding checks remain enabled.

Resolver26's additional root-field-reference property generation uses a fixed schema family surrounded and consumed by ordinary randomized schemas and registries. It provides three nested namespace levels, zero-/one-/four-argument grounded targets, fixed nested inputs, scalar and enum results, concrete-object, interface, and union results, exact two- and four-type output cycles, target Query fragments using `FromArgument` and `FromQueryField`, and resolver extensions on every concrete result type. A forced consumer supplies direct references, a mixed reference/passive list, a three-hop reference tail, and registered-resolver overrides while omitting an active-fallback sibling. Positive insertion weight additionally replaces compatible ordinary generated output positions, including concrete result positions in the middle of output trees. With node resolvers enabled, a dedicated Node resolver returns a reference to a compatible non-Node interface target; generated queries force its ID and resolver-backed extension without relying on the ordinary generated-hash oracle. `resolver26RootFieldReferenceFocused` runs a `10:5:5` product and hard-requires all of those runtime interactions, including generation and activation of the node-resolver reference. Resolver26 deep stress and every persisted broad profile impose the same ordinary and node-reference activation guards, preventing a generic stress run from silently omitting the feature.

## Execution Harness Boundary

The execution module runs validated GraphQL queries through Resolver26 and uses GraphQL Java for response completion. `EngineTestModule.runQPlanFeatureTest` adapts pre-dispatcher Engine API field and node executors into ordinary suspending qplan fixture functions and awaits the executor SPI directly; it does not construct production dispatchers or data loaders. Resolver26 passes each concrete `FieldResolverTask` explicitly to the registry function as its `ResolutionExecutionContext`; no coroutine-context lookup is involved. The adapter wraps that capability in an invocation-local `EngineExecutionContext`. Query `ctx.query()` converts its Engine selection set into canonical qplan selections and delegates back to the task, which resolves a fresh Query-rooted OER under its own field-task scope while retaining the parent operation's world, variable bindings, cycle checker, binding declarations, and observer. Nested calls, aliases, arguments, variables, and field- and node-executor callers are supported; `ctx.mutation()` remains outside the Query-only model. When a feature test supplies a scoped executable schema, the adapter still builds the reasoning world and registry from the full schema but validates and completes the operation against the scoped schema, allowing resolver RSSes to retain private full-schema fields.

TODO: Cancellation of the public `CompletableFuture` returned by `GraphQL.executeAsync` is not yet connected to the Resolver26 request job. `QPlanExecutionStrategy` observes cancellation of its internal result future, but GraphQL Java returns a downstream dependent future whose cancellation does not propagate upstream; `ExecutionInput.cancel()` is only cooperatively polled and cannot terminate a Resolver26 producer that remains suspended indefinitely. A future execution-boundary change should create an atomic request-cancellation coordinator before calling `GraphQL.executeAsync`, register the request job with it inside the strategy, and return a cancellation-aware public future that signals both the coordinator and `ExecutionInput`; the coordinator must preserve cancellation that races ahead of request-job registration. Coverage should start a never-completing provider, cancel the public request, and verify that its `finally` block runs and its owned promises terminate.

The adapter supports selective and non-selective field and node executors; field arguments; object and Query required selections; `FromArgument` variables through nested input-object paths with nullable traversal; supported singular `FromObjectField` and `FromQueryField` paths; composition of disjoint no-RSS variables-provider callbacks into one provider per field resolver; synchronous scalar, enum, list, object, error, node-reference, and root-field-reference outputs; built-in node lookup; namespace fields; and typename completion. It rejects batching field and node executors because qplan does not yet model production batching. Root-field references are recursively normalized inside object and list output before entering Resolver26. Each resolver occurrence receives the Query value materialized from its Resolver26 Query fragment. Missing nullable Query executors resolve to null, while missing non-null Query executors resolve to an error. Lossy abstract-type traversal rejected while compiling a from-field provider path is translated to the production-facing `InvalidVariableException`, so the copied union traversal validation test runs unchanged.

The adapter rejects callback providers with overlapping names or their own required selections, from-field providers whose erased production representation ambiguously matches both resolver fragments, checker and type-checker executors (including their object- and Query-rooted required selections), asynchronous EOD outputs, mutations, subscriptions, and custom scalars. Direct inline materialization of a Node-valued field remains outside the modeled execution contract: Node values enter qplan as `Query.node` references. Query fragments retain their independent fresh Query OERs; caching or deduplicating work shared with the primary operation and cross-occurrence batch coalescing remain separate execution-layer concerns.

Selective-node production tests that inspect mutable node-cache state or repeated refetch counts remain disabled in source-faithful form when that state is not part of Resolver26. Current alternatives cover conservative one-shot abstract demand without production's runtime-narrowed second-materialization snapshot, initial node-reference materialization without asserting mutation of the original production reference, reused references as fresh closed occurrences, nested RSS closure with one complete node call rather than two staged fetches, one stable producer result in place of successive cache-covering results, and a stable embedded list whose unmatched item carries an explicit dependency error. Argument-bearing client, RSS, ancestor, and fragment shapes are represented as distinct active field keys, and provider failures retain their consumer attribution without asking selective node output to own argument-bearing fields. Recursive source normalization preserves nested `EngineErrorData` so list-item dependency errors are attributed to their resolver consumers.

Production node-cache consistency tests compare contradictory successive runtime types, object shapes, or exceptions. Their one-shot alternatives retain the same GraphQL nulls, diagnostic class, and exact consumer paths using one stable node result and a consumer-local failure; each asserts one node call.

The inherited arbitrary suites in both selective-field and selective-node production ports construct a generated production `Viaduct` directly and do not expose an `EngineTestModule` adapter surface. They are N/A for this pre-dispatcher harness rather than feature blockers; qplan's generated property suites own randomized resolver coverage.

Selective-field tests that only assert production materialization source identity or the count of staged GraphQL field-fetch instrumentation callbacks are N/A. They do not add resolver-result coverage and conflict with the one-shot boundary in the same way as the intentionally omitted instrumentation feature-test files.

The latest feature-test enablement pass reduced the 43 non-`AccessChk`/`ParentFld`/`ErrorData` TODO annotations to eleven: nine copied tests now run unchanged, fifteen have passing one-shot alternatives, and eight production-only arbitrary or instrumentation harness annotations are N/A. The retained TODOs are six batching tests, three mutation-root tests, and two custom resolver-directive spelling tests. Batching-focused tests remain disabled unchanged until qplan implements batching; mutation roots are rejected by the current Query-rooted model, and arbitrary directive syntax is not carried by `SelectionForest`, so the latter two categories require model/semantic work outside the execution-only constraint of this pass.

The six selective field and node tests whose intent is to verify batching remain source-faithful and disabled with `TODO: Batching`. Singleton executor calls are not treated as batching coverage; these tests should be enabled unchanged when batching is implemented.

Production-derived execution tests live under `execution/viaductfeaturetests`. Whole-file, source-faithful preservation is the migration policy; unsupported tests remain disabled with coded reasons rather than being rewritten. [`execution/viaduct-feature-test-inventory.md`](./execution/viaduct-feature-test-inventory.md) records the current whole-file exclusions and the port that has drifted from its source file.

## Validation

The full ordinary check passes across model, generators, all maintained resolvers, and execution, including all ten `QueryFragmentBindingTerminationTest` cases. The field-error boundary now completes the current reference hop's bindings, allowing its Query readers to finish after an unexpected exception. JMH sources also compiled in the earlier validation. Run from `qplan/`:

```shell
./gradlew check
```

Earlier stress runs verified 10,000 cases each for Resolver03, Resolver08, Resolver23, and Resolver26 with seed `424242`; Resolver23 was rerun after its task migration.

See the [maintainer guide](./maintainer-guide.md#fast-validation-loop) for focused checks and replay, and [Resolver26 testing](./semantics/src/main/kotlin/semantics/resolver26/testing-resolver26.md) for separate stress and concurrency checks.

## Scope

The executor feature-test adapter is a pre-dispatcher integration surface, not a production `execution2` implementation. Future work must preserve resolver scheduling, response-key materialization, OER identity, variable occurrence identity, query-value occurrence identity, and the explicit source/lowered schema boundary. Dispatcher and data-loader integration, production `execution2` integration, custom scalars, mutations, and subscriptions remain separate work.
